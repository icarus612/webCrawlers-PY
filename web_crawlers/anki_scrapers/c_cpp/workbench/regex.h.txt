<b>Standard Library Function:</b> ['regcomp(regex_t', 'const', 'int'] | <b>Standard library function</b> used to compiles the source regular expression pointed to by pattern into an executable version and stores it in the location pointed to by preg.. | int regcomp(regex_t *preg, const char *pattern, int cflags); 
<b>Standard Library Function:</b> ['regerror(int', 'const', 'char', ''] | <b>Standard library function</b> used to finds the description for the error code errcode for the regular expression  preg.. | size_t regerror(int errcode, const regex_t *preg, char *errbuf,  size_t errbuf_size); 
<b>Standard Library Function:</b> ['regexec(const', 'const', 'size_t', 'regmatch_t', 'int'] | <b>Standard library function</b> used to compares the null-ended string string against the compiled regular expression preg to find a match between the two.. | int regexec(const regex_t *preg, const char *string, size_t nmatch, regmatch_t *pmatch, int eflags); 
<b>Standard Library Function:</b> ['regfree(regex_t'] | <b>Standard library function</b> used to frees any memory that was allocated by regcomp to implement the regular expression preg.. | void regfree(regex_t *preg); 
